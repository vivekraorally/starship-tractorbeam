package com.rallyhealth.starship.tractorbeam.model

import com.rallyhealth.starship.model._

object tractorbeamToolInfo {
  // keep sections in alpha order
  val Main = ToolSection("Main")

  // keep actions in alpha order, by section
  // Main actions
  val Gettractorbeams = ToolAction("Gettractorbeams")
  val Index = ToolAction("Index")
}
